apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
    # Switched via Codefresh variable from blue to green
    version: ${ACTIVE_COLOR} 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
